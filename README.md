# Baseball Limits 2D

## Overview

**[Baseball Limits 2D](https://eliogovea.github.io/baseball-limits-2d/)** visualizes the Pareto frontier of MLB batting and pitching statistics — pick any two dimensions and see which player-seasons (or careers) define the outer edge of what's ever been done.

Per-season and career-totals modes; filters for league, handedness, country of birth, and plate appearances or innings pitched; click any frontier dot to trace that player's full career arc.

## Data sources

All stats are from the [SABR Lahman Baseball Database](https://sabr.org/lahman-database/) (1871–2025 release), distributed under the [Creative Commons Attribution-ShareAlike 3.0 license](https://creativecommons.org/licenses/by-sa/3.0/).

Three tables are used:
- `Batting.csv` — per-player-season batting stats
- `Pitching.csv` — per-player-season pitching stats
- `People.csv` — player names, birth dates, country, handedness, height, weight

Why nothing from the in-progress current season? Lahman publishes once a year (~December/January), so we're current through the most recent completed season. Other public sources (MLB Stats API, Baseball Reference, FanGraphs) either restrict bulk redistribution or prohibit scraping — none of them is compatible with shipping the data inside this open-source site. We'll pick up the next season when SABR releases the following year's Lahman snapshot.

The CC BY-SA license carries over to derivative data we ship, including the canonical `data/*_limits_*.csv` files and the binary blobs inlined in `dist/index.html`. See [LICENSE-DATA.md](LICENSE-DATA.md) for the full attribution + license text. The source code (everything outside `data/` and the inline binary in the bundle) is MIT-licensed; see [LICENSE](LICENSE).

## Update data

When SABR releases a new Lahman snapshot:

1. Download the CSV release ZIP from [sabr.org/lahman-database](https://sabr.org/lahman-database/) and extract into `data/`:

   ```
   unzip -j data/lahman_1871-<YEAR>_csv.zip \
     "lahman_1871-<YEAR>_csv/Batting.csv" \
     "lahman_1871-<YEAR>_csv/Pitching.csv" \
     "lahman_1871-<YEAR>_csv/People.csv" -d data/
   mv data/Batting.csv  data/batting_lahman_1871-<YEAR>.csv
   mv data/Pitching.csv data/pitching_lahman_1871-<YEAR>.csv
   mv data/People.csv   data/people_lahman_1871-<YEAR>.csv
   ```

2. Regenerate the canonical limit CSVs:

   ```
   python3 scripts/convert_csv_lahman.py \
     data/people_lahman_1871-<YEAR>.csv \
     data/batting_lahman_1871-<YEAR>.csv \
     data/batting_limits_1871-<YEAR>.csv

   python3 scripts/convert_csv_lahman_pitching.py \
     data/people_lahman_1871-<YEAR>.csv \
     data/pitching_lahman_1871-<YEAR>.csv \
     data/pitching_limits_1871-<YEAR>.csv
   ```

3. Update the input paths in `script.js` and `scripts/build_bundle.py` to point at the new files.

4. Rebuild the self-contained bundle:

   ```
   python3 scripts/build_bundle.py
   ```

See [CLAUDE.md](CLAUDE.md) for the broader architecture and dev workflow.

## Ideas & future work

Status convention: `- [ ]` open, `- [x] *(shipped <sha>)*` shipped. Items with a design but no implementation note the design doc inline. Update this list in the same commit that ships or moots an item — see CLAUDE.md §"Working methodology" for why this is mandatory.

### Analytical / Pareto frontier concepts

- [x] **Pareto depth (onion peeling)** *(shipped a54bd6a)* — recursively peels the frontier and re-sweeps the remainder up to 5 times, rendering each layer as a staircase behind the live frontier with a stacking translucent fill that deepens toward the inner layers (a topographic map of dominance). "Layers" selector (1–5) in the Plot band; deeper layers are non-interactive; state rides in the hash as `d=<n>`. Implements the onion-peeling design (`docs/pareto-onion-peeling-design.md`, git history; summary in [`docs/rendering.md`](docs/rendering.md)); invariants (depth-1 identity, disjoint coverage) verified via `window.__bl2d_depthLayers`. From the Claude Design upgrades.

- [ ] **Hypervolume shading** — fill the area "dominated" by the frontier (the region beneath and to the left of the curve) with a light gradient. The shaded region is the hypervolume indicator — a single scalar capturing how much of the objective space the frontier controls. Pairs perfectly with the ▶ animation: you can watch the shaded area grow as history advances.

- [x] **Hypervolume contribution per frontier point** *(shipped 6e543a1)* — for each red dot, compute how much the total dominated area would shrink if that point were removed. High contribution = the point "owns" a large exclusive territory on the frontier. A natural complement to the Loneliness Radius: Loneliness measures nearest-neighbour distance in the cloud; hypervolume contribution measures the frontier point's structural importance. Toggle on/off via the "Show HV" checkbox in Filters; URL hash `hv=1`. Encoded as red-dot radius (sqrt(contribution / max)). The two unchosen encodings — saturation/opacity ramp on the red fill, and contribution-scaled halo rings — remain open follow-ups that would compose with this metric.

- [ ] **Crowding distance on the frontier** — the standard NSGA-II metric: for each frontier point, the sum of distances to its immediate left and right neighbours along the frontier. High crowding distance = the point sits in a sparse, uncrowded region of the frontier curve. Could be encoded as dot size or saturation on the frontier, immediately showing which seasons occupy distinct niches vs. cluster together.

- [ ] **Frontier longevity / years held** — for each frontier point, track how many seasons it has stood as the record (and whether it still does). Henderson's 130 SB in 1982 has been untouched for 40+ years; Maris's 61 HR stood 37 years. A "time-on-frontier" color scale — older = more saturated — would make durability legible at a glance.

- [ ] **Era-normalised frontier** — divide each stat by that season's league average (like ERA+, OPS+). The raw frontier is dominated by the steroid era for power stats and the dead-ball era for pitching volume. An era-adjusted view would show who was most exceptional *relative to their peers*, which is a different (and arguably fairer) question.

- [ ] **"Almost frontier" band** — a faint second layer just inside the frontier showing seasons within, say, 5% of both axis values simultaneously. Shows how deep the talent pool is right behind the record-holders and makes the frontier's exclusivity visible.

- [x] **Distance-to-frontier for non-frontier points** — for every season *off* the frontier, compute the shortest objective-space distance to the nearest red dot (Euclidean, dominance distance, or an additive/multiplicative epsilon indicator). Encode as cloud-dot opacity or surface as a "regret" tooltip line — e.g. "Mike Trout 2018 was 4 HR and 0.012 AVG away from the frontier." Turns the background cloud into a heatmap of near-misses. *(shipped 7b9a80e)*

- [ ] **Knee-point / curvature highlight** — flag frontier seasons where the curve bends sharpest. Knee points are the "sweet spot" records: small sacrifice on either axis for a large gain on the other, often the most interesting tradeoffs in a discussion. Compute via local curvature or the angle between adjacent frontier segments and emphasise those dots with a halo or label.

- [ ] **Marginal tradeoff slope at each frontier point** — show the local exchange rate between objectives ("at this point, +1 HR costs roughly -0.004 AVG"). Could appear as a tangent line on hover or as a small annotation on each frontier card. Makes the geometric meaning of the curve concrete in baseball units rather than abstract Pareto-speak.

- [ ] **Convex hull vs. concave frontier points** — distinguish "supported" frontier points (those on the convex hull, reachable by any linear utility weighting) from "unsupported" ones sitting in concave dips. Concave-region seasons are often the most distinctive because no scalar weighted average of the two stats would have surfaced them — they're records that only multi-objective thinking finds.

- [x] **Era-vs-era frontier comparison** *(shipped 771dae6)* — a "Compare a second era" toggle overlays a second year range's frontier (teal, dashed) over the same attribute filters and headlines a coverage % = how much of the comparison era's objective space the primary era dominates (grid-sampled, sign-aware). The axis domain widens to fit both; state rides in the hash (`c2/sy2/ey2`). From the Claude Design upgrades.

- [ ] **Frontier entropy / tradeoff diversity** — a single scalar summarising how spread-out the frontier's tradeoffs are along its length. High entropy = a long, balanced frontier with many distinct niches; low entropy = a short frontier crowded near one extreme. Plotted as a time series alongside the ▶ animation, it would show eras when the sport allowed many different paths to greatness vs. eras when one archetype dominated.

- [ ] **Pareto dominance count per season** — for each point, store how many other seasons strictly dominate it (lower is better) and how many it dominates (higher is better). Surfaces "second-place" and "near-elite" seasons that the binary frontier/non-frontier split currently hides; pairs naturally with the Pareto depth idea above as a continuous companion to the discrete layer count.

### Visual / interaction improvements

- [ ] **Kernel density contour lines** — overlay smooth topographic contours on the point cloud using `d3-contour`. Makes the shape of the distribution legible (where do most qualified seasons cluster?) without obscuring individual dots. Especially useful for dense axes like AVG or ERA.

- [ ] **Voronoi overlay for frontier points** — partition the chart space into cells, one per frontier point, each cell showing the region "closest" to that frontier dot. Visually answers: "if a new season entered, which frontier record would it challenge?" Could be a toggleable layer.

- [x] **Animated transitions on filter change** *(shipped 8194d33)* — dots glide (FLIP) and the outgoing frontier staircase ghosts out (dashed, fading) while the new one redraws, so the frontier "rewrites itself" instead of snapping. Honors `prefers-reduced-motion`; a point-count guard snaps the cloud above 6000 (staircase still morphs). From the Claude Design upgrades.

- [x] **Frontier leaderboard** *(shipped f0e21e0)* — the "On the frontier" sidebar list is now a browsable, ranked leaderboard (name · year · X/Y · % area), click/Enter to pin a player's gold trail, ↑/↓ to navigate. Re-enables the sidebar split so it stays visible. (The career sparkline this item originally imagined now lives in the player spotlight card, where there's room to read it — see below.) From the Claude Design upgrades.

- [x] **Player spotlight card** *(shipped 2a7111a, refined 0e8c0d5)* — pinning a single player opens a card with monogram, career span/handedness, frontier-season count, % of frontier area owned (summed hypervolume contribution), two stacked career sparklines (the X-stat and Y-stat over the player's years, sharing one timeline with frontier seasons ticked on both), and the record seasons. One card per pinned player (in the player's trail color); anchored opposite the frontier and draggable by its header. From the Claude Design upgrades.

- [x] **Curated story presets** *(shipped efbd971)* — one-tap famous frontiers (Ohtani 50/50, Bonds' 73, Henderson 130, Sosa '98, Pedro 2000) that load axes + dataset + a pinned player via the URL-hash path. Presented as a "jump straight into a famous frontier" list in the welcome modal (reachable anytime via Help). Each preset is verified to land its player on the chosen frontier. From the Claude Design upgrades.

- [x] **Dark mode** *(shipped e9eec27)* — shipped as the **Night** theme in a 3-theme system (Classic / Editorial / Night), selectable from the header swatch switcher and persisted in `localStorage`. The holistic pass the earlier attempts lacked: every literal color was first tokenized (`--glass-bg`, `--gold`, `--league-al/-nl`, `--on-primary`), and `applyTheme()` re-skins both the CSS-var chrome and the D3-painted chart (by mutating the in-place `ERAS`/`COLOR_PALETTES` tables). Night uses a near-black field with a light frontier staircase and a blue sequential era ramp.

- [ ] **Canvas rendering** — switch the point-cloud layer from SVG circles to an HTML Canvas overlay (D3 still manages axes, labels, and interactions in SVG). Unlocks smooth rendering with 50 k+ points and removes the current lag at large datasets.

- [ ] **WebGPU compute-accumulate renderer + JS/WASM incremental frontier** — the scaling path toward pitch-by-pitch volumes, and the still-pending web incremental-frontier math (the Pareto sweep currently runs full each frame). **Standalone POC implemented** in [`poc-webgpu/`](poc-webgpu) (the browser twin of [`poc-vulkan/`](poc-vulkan)): a compute shader `atomicAdd`s each frame's new event window into per-player `(HR, SB)` GPU counters, the vertex shader renders straight from them (instanced quads, no readback), and the incremental Pareto frontier is maintained in a WASM core (`core.c` = poc-vulkan's host logic compiled via emcc, reused verbatim) — career **and** season open-year. Verified headless via `poc-webgpu/snap-webgpu.js`: incremental frontier == brute-force (career `frontierMis 0`, all-time frontier = Bonds 762/514 + Henderson 296/1406); GPU counters == WASM shadow (`counterMis 0`); season combined frontier == an independent full Pareto sweep (`frontierMis 0`, single-season records HR 73 / SB 138). **Option 2 (all-C) also implemented** in [`poc-webgpu-c/`](poc-webgpu-c): the *whole* app including the WebGPU orchestration written in C against `<webgpu/webgpu.h>` (emscripten's emdawnwebgpu port), reusing the **same `core.c`** — same invariants pass (career `counterMis 0`/`frontierMis 0`, season `frontierMis 0`). Design in `docs/poc-webgpu-design.md` (git history; see [`docs/rendering.md`](docs/rendering.md)). **Phase 1 of the main-app integration implemented** — the JS incremental frontier now drives the `.evt` **career** streaming path (the design doc's backend-agnostic Phase-2 fix): a flat date-sorted event stream derived from the per-player series (`buildEvtEventStream`), a sign-aware port of `core.c`'s `frontier_apply_event` (`createIncrementalFrontier`), and `buildEvtIncrementalFrontier` swapped in at the frontier seam — replacing the per-frame full sweep with an O(window + frontier) replay (reset + replay-forward on backward seek). Gated to counting axes, falling through to the full sweep otherwise. **Slice 3 also implemented** — the incremental path now **honors the bats/country filters** via a static per-player eligibility mask that gates which players' events feed the engine (resetting + replaying on a filter change), so filtered evt-career views get the incremental win too instead of falling back. Verified with `?verifyFrontier=1` (incremental == full sweep, `mis 0` across a forward+backward scrub, under bats=L, country=D.R., and live filter flips) and the standing records (all-time HR×SB frontier = Bonds 762/514 + Henderson 296/1406); before/after renders pixel-identical, filtered and unfiltered. **Phase 2 (the `PointRenderer` seam) implemented** — the Canvas-2D point-cloud rendering is now lifted behind a `Canvas2DRenderer` class (a `PointRenderer` interface: `resize/clear/drawBackground/drawForeground/drawTrails/drawFrontierDots/bgCanvas/fgCanvas`) that owns the bg/fg canvas layers + the background cache key, so a future `WebGPURenderer` can slot in behind the same method surface. Pure no-op refactor: verified byte-identical snapshots (static season desktop+mobile, career) and visual parity on the streaming + group-career (trails→heads compositing) paths; export still embeds both canvas layers; the bundle stays Canvas-2D-only. **Phase 3 (the `WebGPURenderer` backend) implemented** — a second `PointRenderer` implementation behind `?renderer=webgpu`: one `<canvas>` + an offscreen render target, instanced-quad **points** (per-instance px/radius/ring/rgba vertex-pulled from a storage buffer; CSS-px→NDC with +Y flip; disc-test + white-ring fragment with ~1px AA) and a **line** pipeline for group-career trails, composited in a single `present()` pass in the Canvas-2D layer order. It can be toggled live from a visible **"GPU" pill in the header** (or the `?renderer=webgpu` flag at load, which the toggle keeps in sync for shareable/reloadable state). An async `enableWebGPU` ladder swaps it in only when every capability rung passes (`navigator.gpu` → adapter → device → init), with a `device.lost`/error fallback back to Canvas 2D (the toggle reflects the live backend, so it can't lie after an automatic fallback); the **headless guard** (no canvas configure under `HeadlessChrome`/`webdriver`) keeps the default app's `snap.js` verification on Canvas 2D. Export under WebGPU is a texture readback (`exportDataURLs`). Verified: default path **byte-identical** with and without the flag; `?renderer=webgpu` under headless stays `canvas2d`; the WebGPU backend (SwiftShader, via `poc-webgpu/snap-webgpu.js` + `?webgpuHeadless=1`) renders the cloud + frontier dots offscreen (readback non-empty, frontier = Bonds 762/514 + Henderson 296/1406) and the trail pipeline runs under group-career — no device loss/GPU errors; bundle stays functionally Canvas-2D-only (the WGSL/`WebGPURenderer` ride along inert, never reached without the flag). Full design + data flows in [`docs/rendering.md`](docs/rendering.md) §"Phase 3". **Phase 4 (GPU compute-accumulate) implemented** — the POC's `accumulate.wgsl` is now in the app: during the `.evt` play-by-play **career** animation the high-cardinality cloud is accumulated on the GPU (a `@workgroup_size(64)` compute shader `atomicAdd`s each frame's new event window into per-player running totals) and drawn by a vertex-pull pipeline that reads those totals straight from the storage buffers (no per-frame CPU `evtPointsAsOf`/flatten, no readback), composited in `present()` ahead of the fg/frontier draws. Generalised beyond the POC's fixed HR/SB to **any monotone-nondecreasing linear counting axis** — single (HR, SB, R, …) **and** composite (TB = 1·H+2·2B+3·3B+4·HR, PA, …) — by probing each axis's per-component coefficients (`coeff = fn(unit)−fn(0)`) and baking the axis-space delta into each event; the GPU tracks its own `applied` cursor so it stays correct across the non-lite frames it skips. The JS incremental frontier stays authoritative (frontier dots/staircase/tooltips/cards); the GPU only replaces the cloud. Gated to `?renderer=webgpu` + evt-career + monotone counting axes + era colour + no attribute filter + a playback ("lite") frame; **rate stats (AVG/OBP/SLG — they can decrease) and any non-monotone combo fall back to the CPU cloud**, as do paused/idle frames (so hover + the quadtree hit-test keep working on real point objects). Verified headless (SwiftShader, `poc-webgpu/snap-webgpu.js` + `?webgpuHeadless=1`): GPU counters == the JS shadow (`counterMis 0`) for HR×SB **and** TB×SB; record spot-checks straight off the GPU buffer (Bonds 762 HR / 514 SB / 5976 TB, Henderson 296 HR / 1406 SB); incremental frontier == brute sweep (`verifyFrontier mis 0`); the offscreen readback is non-empty + axis-aligned; a rate axis correctly stays `webgpu`-renderer but `__bl2d_evtGpuCloud === false`; the default Canvas-2D path is unchanged. Measured Canvas-2D vs WebGPU frame times in `docs/renderer-performance.md` (git history; summary in [`docs/rendering.md`](docs/rendering.md)) (GPU render cost is flat in cloud size vs Canvas's linear ~0.2 ms/1k dots; total still bounded by the shared on-CPU frontier math). Remaining (stretch): GPU frontier staircase and the deferred `core.wasm` path at pitch-by-pitch volumes; honoring the bats/country eligibility mask on the GPU (a filter currently falls back); plus the incremental `.evt` **season** path (completed-cache + open-year merge — a smaller win, since the season sweep is already O(n) over a cached completed set). **Phase 5 (spring motion + a fully-GPU streaming engine) implemented** — the [`poc-webgpu-spring`](poc-webgpu-spring) pipeline brought into the app as a **second, explicit streaming engine** behind `?renderer=webgpu&gpustream=1` (the shipped Phase-4 hybrid stays the default under `?renderer=webgpu` alone). When active on an `.evt`-career lite frame, the GPU owns the whole picture: a critically-damped **spring** pass glides each player's `pos[]` toward the accumulated counters (the stable polynomial-`e` integrator, no overshoot); a per-frame brute-force O(n²) **skyline** pass recomputes the Pareto frontier (`onFront[]`) over the smoothed positions; and a **fully-GPU staircase** (`compact`→`ranksort`→`emit`→`drawIndirect`, vertex count written by the GPU, never plumbed through JS) draws the red step-line. The spring cloud vertex-pulls `pos[]` and reads `onFront[]` to draw its own larger ringed frontier dots (two-pass so they sit on top); the CPU frontier dots + the SVG staircase are suppressed in this path. CPU↔GPU traffic is minimal: the event stream is uploaded **once**, per frame only ~32 bytes of uniforms cross the bus (`{lo,count}` + `{dt,omega}`), and there is **zero per-frame GPU→CPU readback** (the headless device-loss trigger) — the only `mapAsync` calls are the one-shot verify hooks. Verified headless (SwiftShader, `poc-webgpu/snap-webgpu.js` + `?gpustream=1&webgpuHeadless=1`, `__bl2d_verifySpring`): drive to end-of-history, settle in one huge-Δt step, read back once — **springMis 0** (settled `pos` == integer counter within ½ unit), **skylineMis 0** (GPU `onFront` == CPU brute-force Pareto), all-time frontier size 2 = Bonds 762/514 + Henderson 296/1406 (both on-front); GPU staircase `indirect.vertexCount` = `1+2K` with the correct step vertices; default Canvas-2D path unchanged. Design + the "two engines, not a hybrid" rationale in [`docs/rendering.md`](docs/rendering.md) §"Phase 5". Remaining: the JS incremental frontier still **runs** to feed DOM/interaction (cards, the quadtree hit-test, labels) rather than being fully replaced by a throttled idle `onFront` readback — that reconciliation, plus a crisper (quad-based) staircase line and honouring the bats/country mask on the GPU, are the follow-ups. **UI + smoothness pass:** a header **spring** toggle now surfaces `?gpustream=1` (previously URL-only; it rebuilds the renderer so `springMode` takes effect, and hides under Canvas 2D); the GPU `present()` is decoupled from the ~15fps playback throttle (`PBP_PLAY_FRAME_MS`) onto a dedicated **display-refresh glide loop** (`presentGlide` + a `SPRING_SETTLE_MS` settle tail) so the spring animates at the panel's full rate (60 Hz, or 120 Hz on ProMotion) like the standalone POC instead of stepping at 15fps; and the SVG HV-shade is suppressed under `gpuSpring` (it was built from the *settled* CPU frontier and so led the gliding GPU staircase — the area appeared ahead of the red line). **Season-mode GPU animation** (extending the spring engine past career) is designed phased + resumable in [`docs/rendering.md`](docs/rendering.md) §"Season GPU animation"; implementation pending — career stays the only GPU-smoothed mode for now (season still renders on the CPU ~15 fps path, since `filters.evt` only gates the evt-**career** cloud). The key reuse: a season value is the career-cumulative GPU counter differenced against the open season's start, so ~90% of the accumulate→spring→skyline→staircase pipeline carries over with a per-year baseline.

- [ ] **Full-GPU graph rendering (G-track)** — extend the GPU work from the streaming animation to the *whole* chart: a retained-scene WebGPU renderer (data-space buffers + a scale uniform, so zoom/resize is a uniform write) that draws every static season/career view — dots, sign-aware GPU Pareto frontier (any stat pair, incl. lower-is-better), staircase, HV shade **and** GPU hypervolume contributions, depth layers, era-B/ghost overlays, plus glyph-atlas **text** (axes, ticks, titles, on-chart labels) — with a single damage-flag render loop, a non-blocking frontier readback feeding the DOM cards, and the existing `gpustream` spring engine converging in as a special case (its `pos[]` is just a scene produced by compute instead of upload). Canvas2D+SVG stays as the permanent fallback. Phased G0–G6, each with a headless `__bl2d_verifyGraph` invariant gate. *(Design in [`docs/rendering.md`](docs/rendering.md) §"G-track design".)* **G0 implemented** — the retained-scene dot renderer: a new [`webgpu-graph.js`](webgpu-graph.js) attaches the scene engine to `WebGPURenderer` (script.js gains only 4 optional-chained hooks + the `gpuGraph` gate), uploading the static background cloud ONCE per identity change as data-space SoA buffers (`bScenePos`/`bSceneColor`/`bSceneSize`) and mapping data→pixels per vertex from a 64-byte `uScene` uniform — so a zoom/resize/redraw with the same point set re-uploads nothing (verified: `uploads` stays 1 across an identity-preserving redraw, bumps exactly once on a year-range change). The bundler inlines the new file tour.js-style. Verified headless (SwiftShader, `poc-webgpu/snap-webgpu.js`, on the `file://` bundle — the static path's natural habitat, since smooth auto-enables on every .evt-covered axis pair in the multi-file app): `__bl2d_verifyGraph` → dotCount == filtered N (1907), GPU position readback == uploaded floats exactly (`posMis 0`), offscreen render visually matches the Canvas-2D cloud; default path byte-identical (mobile) / AA-noise-only (desktop); `__bl2d_verifySpring` regression green (springMis 0, skylineMis 0, Bonds 762/514 + Henderson 296/1406). **G1 implemented** — the sign-aware GPU Pareto frontier over the retained scene plus the readback contract's mechanism: the scene now uploads the FULL deduped cloud (`unique`, frontier included; the cloud shader degenerates on-front instances, so the visible output is unchanged until G2), a `sceneSkyline` compute pass brute-forces strict dominance in signed space (xSign/ySign folded into the compare, exactly matching the CPU sweep on the deduped set; `originDrop` compiled in but gated off for static scenes), a `compact` pass atomic-appends on-front indices into `bFrontIdx`+`bCount`, and a double-buffered fire-and-forget `mapAsync` lands the result on renderer state (`g.front`) on scene-dirty frames only — render never waits on a map; cards/tooltip/quadtree stay CPU-fed until the G5 convergence. Verified headless (SwiftShader, `poc-webgpu/snap-webgpu.js`, on the `file://` bundle): `__bl2d_verifyGraph` → skylineMis 0 / frontMis 0 / cardPidsMatch true across 20 random axis combos including ERA↓, BB/9↓ and the worst-frontier toggle (GPU readback set == the sidebar cards' frontier as a frounded-(x,y) multiset); posMis 0; `uploads` and `frontReads` both stay 1 across identity-preserving redraws (the skyline only runs on scene-dirty frames); Henderson on the HR×SB frontier; the offscreen texture shows the full cloud + frontier dots with no doubled/missing dots; gpustream `__bl2d_verifySpring` regression green (springMis 0, skylineMis 0, Bonds 762/514 + Henderson 296/1406). The evtSeason completed-seasons bg-layer routing was considered and deferred to the S-track (SA2). **G2 implemented** — the GPU now owns the entire static envelope (everything but text): a **sign-aware GPU staircase** (`ranksort`→`emit`, ranking by canonical x and capping the step run at the canonical *domain* edges — not value 0 — then un-folding each vertex back to data space so a zoom is still just a `uScene` write), a **gradient HV shade** (a triangle fan from the anti-ideal apex over the staircase, the fragment projecting onto the ideal→anti axis from `uScene.corn` to reproduce the SVG linearGradient), **HV-sized white-ringed frontier dots** (the cloud shader's on-front degenerate handed to a dedicated `sceneFront` pass that pulls a per-instance radius), and — the crux — **GPU hypervolume contributions ported as the EXACT leave-one-out-with-fill oracle** (`computeHvContributions`): one GPU thread per frontier slot re-sweeps the whole cloud in canonical space *excluding* that point (ΔHV = totalHv − altHv), rather than the cheap exclusive-corner formula which diverges whenever a cloud point sits behind a frontier member and would change the on-screen dot radii. The CPU SVG shade/staircase and `drawFrontierDots` are suppressed under `gpuGraph`. Verified headless (SwiftShader, `poc-webgpu/snap-webgpu.js`, on the `file://` bundle) across HR×SB, ERA↓×SO, WHIP↓×SO and the worst-frontier toggle: `radiusMis 0` (the visible dot size — the authoritative parity gate), `stairVertMis 0` (`1+2K` vertices), `shadeQuadrant` flips correctly with the signs; `hvMis 0` at a maxContrib-normalized f32 tolerance (a contribution is total−alt of two large HV areas, so the design's raw 1e-6 is unreachable — but the radius's sqrt compresses the residual away); G0/G1 invariants stay green and `__bl2d_verifySpring` regression green. **G3 implemented** — the renderer's first sampled texture: a Canvas2D-rasterized **glyph atlas** (NOT SDF) covering digits/ASCII/the Latin-1 letter block (so player-name diacritics — Martínez/Peña — are covered without enumerating 24k names; `uploadText` lazy-rebuilds on a codepoint cache-miss) draws the **axis tick labels + on-chart frontier names** as instanced textured quads (vertex-pulled glyph records in CSS-px with the margin folded in, premultiplied output matching the G-track src-over blend, a white halo + coloured fill as two instances per label glyph to reproduce `.frontier-label`'s paint-order). Tick generation (d3 `.ticks()`/`.tickFormat()`) and label collision layout (`layoutFrontierLabels`, reused verbatim) stay CPU — the GPU just draws the laid-out quads. Atlas upload is `writeTexture(getImageData)` (reliable on every backend, where `copyExternalImageToTexture` can silently yield a blank texture under headless). The SVG tick `<text>` is kept for a11y but set `fill: transparent` under `body.gpugraph`; axis TITLES + leader LINES + tick MARKS stay SVG (titles keep glossary interaction; the rotated Y-title defers to G5). Verified headless (`poc-webgpu/snap-webgpu.js` on the `file://` bundle) across season HR×SB, pitching ERA↓×SO, career, the worst-frontier toggle and AVG×HR: `__bl2d_verifyGraph` → `glyphMis 0` (instance count == Σ-codepoints, ×2 for halo'd labels), `tickMis 0` (GPU tick strings == d3's default format over the live scales), `atlasMissing 0` (every requested codepoint has an atlas cell — no `.notdef`); G0–G2 invariants stay green (`skylineMis`/`frontMis`/`radiusMis`/`stairVertMis`/`hvMis` all 0) and `__bl2d_verifySpring` regression green (springMis 0, skylineMis 0, Bonds 762/514 + Henderson 296/1406); default Canvas/SVG path unchanged. **G4 in progress** — the **GPU depth-layer peeling compute** landed (the correctness-critical half; rendering migration pending, mirroring how G1 shipped the frontier compute before G2 drew it). Iterative skyline peeling (`WEBGPU_DEPTH_WGSL`: `peelInit`/`peelSky`/`peelMark`, ≤5 layers, dirty-frames only): `peeled[]`/`layerOf[]` cloud-sized scratch, one peel iteration = peelSky (this layer's frontier over the non-peeled set, sign-aware, same compare as the G1 skyline) → peelMark (fold into `layerOf`+`peeled`); the two-pass split is a read-after-write guard so every dominance test sees a settled `peeled[]`. The per-iteration layer index rides a **dynamic-offset uniform** (a shared uniform written between passes in one submit would keep only the last queue write). Verified headless (`poc-webgpu/snap-webgpu.js`, file:// bundle) across season (d=2/4/5), career, pitching ERA↓×SO and the worst-frontier toggle: `__bl2d_verifyGraph` → `depthMis 0` (GPU per-layer point counts == the CPU `paretoLayers`/`__bl2d_depthLayers` oracle in every sign quadrant, e.g. season d=5 `[8,15,20,21,25]`, ERA↓ d=3 `[5,11,10]`, career d=3 `[2,7,14]`); G0–G3 invariants stay green (`skylineMis`/`radiusMis`/`glyphMis`/`frontMis` all 0, `uploads` stays 1 on the d=1 default) and `__bl2d_verifySpring` regression green. No visual change yet — the deeper layers still draw via the existing SVG overlay until the G4 render pass (GPU depth dots + staircases) lands. Remaining for G4: render the peeled layers on the GPU (faded dots + staircases, suppress the SVG overlay), then era-B + dashed ghost + cross-fade. G5–G6: the single damage-flag render loop + non-blocking card readback (G5 convergence), and the rotated Y-axis title.

- [ ] **World map birthplace view** — a companion mini-map showing where frontier (or highlighted) players were born, one dot per player. Leverages the existing country/birthplace data already in the dataset.

- [x] **Player search box** *(shipped 7752a0f)* — a typeahead in the filter panel that highlights a player's seasons (colored dots, dimmed cloud) and pins them as a chip. Uses the disambiguated display names already in `playerIndex`. Replaces the "scroll the frontier card list and hope they're on it" flow with a direct lookup. (The box existed earlier but didn't highlight — `pick()` called the closure-scoped `refreshChart`; `7752a0f` routes it through the `bl2d:refresh` event.)

- [x] **Reduced-motion support** *(shipped e9eec27)* — a `@media (prefers-reduced-motion: reduce)` block collapses decorative transitions and the modal pop/fade to instant. The loading spinner is re-exempted (it conveys state), and the user-initiated ▶ frontier animation is left intact as opt-in motion.

- [x] **Theme system (Classic / Editorial / Night)** *(shipped e9eec27)* — header swatch switcher, `localStorage` persistence, and per-theme era ramps. See **Dark mode** above for the architecture. From the Claude Design review.

- [x] **CVD-safe era encoding + legend-reflects-encoding** *(shipped e9eec27)* — the era cloud now uses a luminance-monotonic sequential ramp (protanopia/deuteranopia-safe; the old 7-hue categorical scale wasn't), and the chart legend renders the key for whatever the active **Color-by** encoding is (era colorbar with year ends / AL-NL / handedness) instead of a static league key. Adds an **Era / League / Bats** color-by control (era is now the default, matching the welcome copy); state rides in the URL hash as `cb`. From the Claude Design review.

- [x] **Keyboard focus ring** *(shipped e9eec27)* — a global `:focus-visible` ring restores a visible focus indicator on the custom controls (segmented toggles, chips, icon buttons) that previously stripped the UA outline. From the Claude Design review.

- [x] **Sidebar hierarchy + mobile axis bar** *(shipped e9eec27)* — the controls are grouped into **Plot / Filter / Highlight** bands with the axis selects emphasized, and a persistent compact X-vs-Y bar sits above the mobile drawer so axis switching doesn't require opening it. From the Claude Design review.

- [x] **Frontier-label collision avoidance** *(shipped e9eec27)* — label placement now measures real text width via `getComputedTextLength()` (replacing the per-character estimate) and adds leader lines for dodged labels. Invariant: no two `.frontier-label` boxes overlap after layout (`window.__bl2d_labelOverlaps === 0`). From the Claude Design review.

- [x] **Logomark + favicon / app-icon set** *(shipped e9eec27)* — the product had a wordmark only; added the "apex" mark (Pareto staircase + gold record dot) inline in the header plus `favicon.svg`/PNGs, `apple-touch-icon`, and `manifest.webmanifest`. From the Claude Design review.

- [x] **Axis-label glossary tooltips** *(shipped f192458)* — hover the X/Y axis label to get the same stat blurb the glossary modal shows, without opening the modal. Especially useful on first visit when users don't yet know that ERA or WHIP on the frontier highlights the *worst* seasons (since the frontier finds the upper-right envelope and those stats are "lower is better"). Surfaces the explanation exactly where the confusion happens.

- [ ] **Keyboard navigation across frontier points** — arrow keys cycle through frontier dots left-to-right with focus + tooltip, Enter pins the career highlight, Escape clears it. Makes the chart usable without a mouse, improves accessibility, and gives power users a fast way to scan the whole frontier without precise pointer aim. *(Partially shipped via the frontier leaderboard (`f0e21e0`): rows are focusable, ↑/↓ navigate, Enter pins. The on-**chart** dot arrow-cycle + tooltip is still open.)*

### Data / scope

- [ ] **Intra-season / day-by-day animation** — the current ▶ animation steps by full season. With daily cumulative stats (e.g. running HR total after each game) you could watch a record-breaking season unfold game by game. *Unblocked: Retrosheet's parsed game-level CSVs (1898–2025, CC BY-SA-compatible) are an open source. **Phase 1 implemented** on branch `pbp-animation` (batting, single season): `scripts/convert_retrosheet_pbp.py` emits a compact bit-packed `BL2P` season file (`data/pbp/b1998.bl2p.gz`, lazy-loaded, not inlined into the bundle), and a "Smooth" toggle + date scrubber drives an as-of-date cursor that re-feeds the existing frontier pipeline. Verified: McGwire's 1998 HR climbs to 70 and the season-end frontier matches the Lahman season frontier exactly. Data scope + format rationale in `docs/pbp-data-format.md` (git history; see [`docs/data-formats.md`](docs/data-formats.md)). **Phase 2 implemented** (same branch): the cursor is now a virtual multi-year timeline over the selected year range, and the frontier **accumulates** across seasons — completed years stay on the chart at their full totals while only the open season grows game-by-game, so the all-time-best envelope evolves outward as the cursor sweeps instead of resetting each year. It lazy-loads only the open season (prior years render from the already-loaded season data), prefetches the neighbour, skips 404 gaps, and locks the axes to the full window's final envelope. The full batting corpus `b1920..b2025.bl2p.gz` (~12 MB) is committed and the converter gained a `--dataset` flag. Verified: 1953–1955 boundary crossing (one transition per year, never lands on a gap), deep-link, the single-season frontier == Lahman season frontier on HR×SB, and the accumulating multi-year final == the static multi-year frontier. **Phase 4 implemented** (same branch): a **group-career animation** — select up to 6 players (the existing highlight chips) and hit a "Group careers" toggle to race their *cumulative* careers through stat-space as N moving head-dots with fading trails, axes locked to the group's career-end envelope. Built on the Canvas point layer + `d3-quadtree` hit-testing (the rendering-perf gate, resolved: the wide-window draw dropped from ~90 ms to a few ms; group-career frame p95 ≈ 2 ms). Verified: each member's career-end head equals their Lahman career total on counting axes (Bonds 762 HR / 514 SB, Henderson 297 HR / 1406 SB, Mays 660 HR), and trails are monotonic in counting stats. Design in `docs/pbp-rendering-design.md` (git history; see [`docs/rendering.md`](docs/rendering.md)). Remaining: pitching corpus + read path (the `--dataset pitching` path needs W/L/SV/SHO derivation — see [`docs/ROADMAP.md`](docs/ROADMAP.md) §Backlog).*

- [ ] **Event-level play-by-play corpus (`BL2E`)** — the existing `.bl2p`/`.evt` layers are game-aggregated; this is the true play-level grain (Retrosheet's ~16.5M parsed plays, 1903–2025; a 2-HR game is two rows). **Format spec + converter implemented** (branch `feat/event-level-pbp`): `scripts/convert_retrosheet_events.py` reads Retrosheet's *parsed* `plays.csv` directly (no event-string grammar / Chadwick) and emits one gzipped `data/pbp/e<year>.bl2e.gz` per season — columnar, bit-packed, lazy-loadable like `.bl2p`. **Layer A** (event core: context + outcome enum + outs + runs/RBI + per-entity advance disposition + a 20-bit flags bitfield) reproduces every charted stat *and* the base-out state; **Layer B** stores the raw pitch sequence verbatim (`--no-pitches` to omit). Per the "keep explicit" sizing choice (batter/pitcher/state stored, not replay-derived) it runs ~33 b/event without pitches, ~54 with, projecting to ~84 MB for the full corpus. **Layer C** (opt-in `--fielding`, header flag bit1) adds the full fielding superset: `f2`–`f9`, the six umpires, `loc`/`fseq`/`hittype` (value-coded), and `e1`–`e9`. Tooling: `decode_bl2e.py` (reference reader), `verify_bl2e.py` (count / full round-trip / replay base-out invariants), `crosscheck_bl2e.py` (direction-aware vs Lahman), `build_bl2e_corpus.py` (full-corpus driver). **Full corpus built & committed 1910–2025** (116 files, 76.8 MB, no gaps; 1903–1909 have no regular-season plays). Verified: round-trips exact (incl. pitches + fielding) and replay base-out holds for 100% of events on 2023 + 1955; all 116 seasons pass the Lahman cross-check (modern seasons match exactly). Notable: Retrosheet's parsed plays also carry **Negro-League (~1920–1948) and Federal-League (1914–15) play-by-play**, so BL2E covers those too (e.g. 1937 has the Homestead Grays, KC Monarchs alongside the 16 AL/NL clubs) — ties into the Negro Leagues spotlight idea below. Spec + resumable build-status checklist in [`docs/data-formats.md`](docs/data-formats.md) §BL2E. Remaining: app integration (decode path + intra-game / pitch-level animation).

- [ ] **`BL2S` normalized per-stat layer (supersedes `.evt`)** — a star-schema redesign of the single-stat timeline layer: one shared `stat_players.bl2s.gz` dimension (gpid → retroID, name, birthYear, bats) + one `stat_<name>.bl2s.gz` per counting stat holding only `(gpid, date, count)`, date = days since 1910-04-14 (u16-safe). Encoding (per-player varint date-deltas) and the "one file per stat, don't group" decision were chosen *empirically* ([`scripts/statfile_experiment.py`](scripts/statfile_experiment.py)): varint beats columnar ~2× and CSV ~4×; grouping doesn't help post-gzip. **S1 implemented** ([`scripts/build_stat_files.py`](scripts/build_stat_files.py), [`decode_stat.py`](scripts/decode_stat.py)): the 15 batter-attributed stats (PA/AB/H/2B/3B/HR/RBI/BB/IBB/SO/HBP/SF/SH/GIDP/G) built from BL2E, 1910–2025, ~13.9 MB, verified exact (Bonds 762 HR, 2023 HR 5,868). Data strategy: **Retrosheet primary + Lahman complement** (Lahman kept for pre-1910 / gaps / full Negro Leagues; all converters kept). Full design + decision log in [`docs/data-formats.md`](docs/data-formats.md) §BL2S. **S1b implemented** — the SB/CS/R exact-source decision resolved as plays.csv (Decision 10 option A): `build_stat_files.py` rewritten to source ALL 18 stats from Retrosheet's parsed `plays.csv` directly, runner stats credited to the real `br*_pre`/`run*` identities so substitutions are exact (**Henderson 1,406 SB** — not the ~1,398 a BL2E replay produced); full 1910–2025 rebuild committed (17,698 players, 15.3 MB, first-appearance gpid; 2023 totals == Lahman, Bonds 762 HR, Brock 938 SB, Coleman 752 SB). Remaining: S2 Lahman complement (pre-1910 / gaps / full Negro Leagues), S3 app migration + remove `.evt` *(phased resumable plan — S3a builder dates+pitching layer, S3b batting swap, S3c pitching swap, S3d removal+deploy — in [`docs/ROADMAP.md`](docs/ROADMAP.md) §S3; implementation pending)*.

- [x] **Interactive guided tour** *(shipped 5984102)* — a zero-dependency spotlight tour (`tour.js`) dims the app and lights up one region at a time (chart, frontier, axis labels, story shelf, controls) with a one-line coachmark, step dots, Back/Next/Skip, keyboard, and reduced-motion support. Launched from a "Take the tour" button in the welcome modal (supplements it). From the Claude Design upgrades.

- [ ] **Negro Leagues spotlight mode** — the 2020 Lahman release added Negro Leagues seasons and they're already in the dataset, but the league filter and broad year ranges make them easy to overlook. A dedicated toggle (or league preset) that emphasises Negro Leagues seasons in a distinct colour would surface a slice of baseball history that's currently invisible by default, and would pair well with the era-vs-era frontier comparison idea above.

### Methodology infrastructure follow-ups

These came out of the methodology validation pilots and aren't user-facing features — they make the methodology itself sharper. See CLAUDE.md §"Working methodology" for the framework.

- [x] **Per-branch GitHub Pages preview deploys** *(shipped 84f3600)* — closes the headless-verification gap for WebGPU work (headless Chrome has no GPU adapter, so `snap.js` can't drive the GPU spring/staircase). `.github/workflows/deploy-pages.yml` serves production + a preview of every branch from one Pages site via an additive `gh-pages` branch (`main` → root, others → `/experimental/<branch>/`), shipping only the runtime web assets. One-time setup + the one-web-POC-per-branch reorg guidance in [`docs/ROADMAP.md`](docs/ROADMAP.md) §Infrastructure. When adopted, update CLAUDE.md §"Git / deployment" (production moves from main/root to gh-pages/root).

- [ ] **`snap.js --color-scheme` flag** — wire CDP `Emulation.setEmulatedMedia` for `prefers-color-scheme` so the verification floor can exercise dark-mode (and any future `@media` feature query) headlessly. TODO marker is already in `scripts/snap.js` near `setDeviceMetricsOverride`.

- [ ] **Fill in `~/.claude/pricing.json`** — one-time: copy `scripts/methodology_pricing.example.json` to `~/.claude/pricing.json` and fill in current per-million-token rates from anthropic.com/pricing. `methodology_audit.py`'s $-savings columns will become meaningful instead of $0.

- [ ] **Broaden pilot runner's Bash allowlist** — Sonnet's natural shell style (multi-line, backgrounded `&`) didn't match `Bash(python3 -m http.server *)`. Either pre-ship a `scripts/verify_pilot.sh` helper that pre-allowlists with one path, or document the shell-style constraints in the pilot prompt itself.

- [ ] **Bias glossary popover above the title for X-axis hover** — `positionNear` only flips above when truly off-screen, so X-axis hover lands the popover near the bottom viewport edge. Small `positionNear` tweak.
